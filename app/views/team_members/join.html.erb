<h1>TeamMembers#join</h1>
<p>Find me in app/views/team_members/join.html.erb</p>

<form action="/team_members/join_complete" method="POST">
    <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
    <div>
        소속한 리그를 선택해주세요:
        <select name="league_id">
            <% @leagues.each do |league| %>
    		    <option value="<%= league.id %>"><%= league.league_name %></option>
    	    <% end %>
        </select>
    </div>
    
    <div>
        소속한 팀을 선택해주세요:
        <select name="team_id">
            <% @leagues.each do |league| %>
                <% league.teams.each do |team| %>
                    <option value="<%= team.id %>"><%= league.league_name %> | <%= team.team_name %></option>
                <% end %>
    	    <% end %>
        </select>
    </div>
    
    <div>
        <button type="submit">입력완료</button>
    </div>
</form>